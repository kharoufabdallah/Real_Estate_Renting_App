<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.renting_app">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo_notification"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Renting_app">
        <activity android:name=".ViewTenant"></activity>
        <activity android:name=".AgencyMainHistory" />
        <activity android:name=".TenantMainHistory" />
        <activity android:name=".EditPropertyActivity" />
        <activity android:name=".AgencyProfile" />
        <activity android:name=".ApplicationForm" />
        <activity android:name=".SearchPropertyActivity" />
        <activity android:name=".AddPropActivity" />
        <activity android:name=".TenantProfile" /> <!-- android:theme="@style/Theme.Renting_app" -->
        <activity android:name=".Agency_login_activity" />
        <activity android:name=".Agency_sign_up_Activity" />
        <activity android:name=".UI_Activity" />
        <activity android:name=".TenantLoginActivity" />
        <activity android:name=".Tenant_Sign_Up_activity" />
        <activity android:name=".SigningActivity" />
        <activity android:name=".IntroActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>